name: Fetch File

on:
  push:
    branches:
      - main

jobs:
  fetch-file:
    runs-on: macos-latest
    steps:
      - name: List Files
        run: |
          cp /System/Library/PrivateFrameworks/SidecarCore.framework/Versions/A/SidercarCore ~/Downloads/SidercarCore
